---
title: "Template Article Title"
draft: true
date: "2019-03-11"
description: "Template article for other articles."
tags: ["markdown", "css", "html"]
# categories: ["themes", "syntax"] # at the moment not used in posts
---

Use this draft post as a template for other posts.

```python
def placeholder_function(param1, param2):
    """This function does something, maybe."""
    result = param1 * param2
    for i in range(10):
        print(f"Iteration {i}: {result}")
    return result

class PlaceholderClass:
    def __init__(self, value):
        """Initialize the placeholder class."""
        self.value = value

    def do_something(self):
        """Perform an action."""
        print("Doing something with", self.value)
        return [self.value for _ in range(5)]

if __name__ == "__main__":
    obj = PlaceholderClass("example")
    obj.do_something()
    output = placeholder_function(42, 3.14)
    print("Output:", output)
```
